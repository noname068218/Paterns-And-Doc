# 🎯 Подготовка к собеседованию .NET Middle Developer

## Концептуальная и стратегическая документация

> **Философия**: Это не список определений для заучивания. Это руководство для **глубокого понимания** концепций и **рассуждения** как опытный разработчик.

---

## 📚 СОДЕРЖАНИЕ

### ЧАСТЬ 1: Концептуальные основы
1. [ООП — философия и принципы](#oop-filosofia)
2. [Система типов в C# — архитектурные решения](#tipi-csharp)
3. [Управление памятью — как это работает](#memoria)
4. [LINQ — сила композиции](#linq-potere)

### ЧАСТЬ 2: Асинхронность и многопоточность
1. [Async/Await — неблокирующая модель](#async-rivoluzione)
2. [Многопоточность — координация и синхронизация](#multithreading)
3. [Паттерны конкурентности](#concorrenza-patterns)

### ЧАСТЬ 3: Веб-архитектура (ASP.NET Core)
1. [Pipeline и Middleware](#aspnet-pipeline)
2. [Dependency Injection](#di-filosofia)
3. [REST API — принципы и практика](#rest-api)
4. [Аутентификация и авторизация](#autenticazione)

### ЧАСТЬ 4: Персистентность данных
1. [SQL Server — запросы, индексы, транзакции](#sql-performance)
2. [Entity Framework Core](#ef-core)
3. [Стратегии доступа к данным](#data-access)

### ЧАСТЬ 5: Архитектура ПО
1. [Clean Architecture](#clean-arch)
2. [Принципы SOLID](#solid)
3. [Design Patterns](#patterns)

### ЧАСТЬ 6: Качество и развёртывание
1. [Тестирование](#testing)
2. [Обработка ошибок](#error-handling)
3. [DevOps и CI/CD](#devops)

### ЧАСТЬ 7: Подготовка к собеседованию
1. [Quick Q&A — ответы на типичные вопросы](#domande)
2. [Стратегии коммуникации](#strategie)
3. [Подготовка по резюме](#esempi-cv)

### ЧАСТЬ 8: Kirey — подготовка к собеседованию
1. [Требования, Quartz.NET, WCF, Insurance](#kirey)
2. [Функциональный анализ, soft skills](#kirey)
3. [Чеклист и вопросы для Kirey](#kirey)

### ЧАСТЬ 9: Дополнительные технологии
1. [gRPC vs REST](#grpc-rest)

---

# ЧАСТЬ 1: Концептуальные основы

## 1. ООП — философия и принципы {#oop-filosofia}

### Зачем нужна ООП?

Объектно-ориентированное программирование — это **эволюционный ответ** на базовую проблему: **как организовать сложность** в растущих программных системах?

В ранние годы программы были небольшими и процедурными. Когда системы выросли (тысячи и миллионы строк), понадобился другой подход.

**Проблема**: В процедурном коде изменение одной части может сломать всё. Данные и функции разделены, любая функция может изменить любые данные.

**Решение ООП**: Объединить **данные и поведение** в единицы — «объекты». Каждый объект сам управляет собой.

---

### Четыре столпа ООП

#### **Инкапсуляция**

**Суть**: Инкапсуляция — это скрытие внутренних решений.

При написании класса вы принимаете решения: как хранить данные, какие алгоритмы использовать. Инкапсуляция говорит: «Эти решения **приватны**. Снаружи видна только публичная поверхность».

**Почему это важно**:

Если поле `Balance` публично, можно написать:
```csharp
account.Balance = -1000; // Катастрофа!
```

С инкапсуляцией:

```csharp
public class Account
{
    private decimal _balance; // Скрыто
    
    public void Withdraw(decimal amount)
    {
        if (amount <= 0)
            throw new ArgumentException("Amount must be positive");
        if (amount > _balance)
            throw new InvalidOperationException("Insufficient funds");
        
        _balance -= amount;
    }
    
    public decimal GetBalance() => _balance;
}
```

**Результат**:

1. **Контроль**: Только вы решаете, как меняется баланс
2. **Валидация**: Невозможно создать некорректное состояние
3. **Гибкость**: Можно менять реализацию, не ломая внешний код
4. **Прозрачность**: Все изменения проходят через проверенные точки

---

#### **Наследование**

**Суть**: Наследование моделирует отношения **«является разновидностью»** (IS-A).

Это не только переиспользование кода. Это способ сказать: «Этот концепт — **специализация** более общего».

- `Manager` **является** `Employee`
- `SavingsAccount` **является** `BankAccount`
- `EmailNotification` **является** `Notification`

**Пример**:

```csharp
public abstract class Notification
{
    public string Recipient { get; protected set; }
    public string Message { get; protected set; }
    public DateTime SentAt { get; protected set; }
    
    public abstract Task SendAsync();
    
    protected void LogNotification()
    {
        Console.WriteLine($"Notification sent to {Recipient} at {SentAt}");
    }
}

public class EmailNotification : Notification
{
    public string Subject { get; set; }
    
    public override async Task SendAsync()
    {
        await _emailService.SendEmailAsync(Recipient, Subject, Message);
        SentAt = DateTime.UtcNow;
        LogNotification();
    }
}
```

**Предупреждение**: Наследование создаёт жёсткую связь. Современный принцип — **«Composition over inheritance»**.

---

#### **Полиморфизм**

**Суть**: Возможность работать с разными объектами через **общий интерфейс**.

**Без полиморфизма**:

```csharp
if (payment is CreditCardPayment) ProcessCreditCard(...);
else if (payment is PayPalPayment) ProcessPayPal(...);
// Каждый новый тип — новый if/else
```

**С полиморфизмом**:

```csharp
public interface IPaymentMethod
{
    Task<PaymentResult> ProcessAsync(decimal amount);
}

// Один метод для всех типов платёжных методов
var result = await payment.ProcessAsync(amount);
```

---

#### **Абстракция**

**Суть**: Моделировать **существенное**, игнорируя детали.

**Abstract Class vs Interface** — типичный вопрос на собеседовании.

**Abstract Class** — когда есть **семейство с общей логикой**:
- Общая реализация методов
- Шаблон процесса (Template Method)
- Общее состояние

**Interface** — когда определяете **способности**:
- Только контракт
- Множественная реализация
- Слабую связанность (DI, тестирование)

**На собеседовании**:
> «Использую Abstract Class при иерархии с общей логикой и явной связью IS-A. Interface — для контрактов и Dependency Injection. На практике чаще выбираю Interface для слабой связанности».

---

### Sealed — финальный контроль

`sealed` запрещает наследование.

**Когда использовать**:
1. **Безопасность**: критичные классы (криптография, аутентификация)
2. **Производительность**: оптимизации компилятора
3. **Замысел**: «Класс завершён, не расширяйте его»

---

## 2. Система типов в C# {#tipi-csharp}

### Class vs Struct — базовый выбор

**Ключевой вопрос**: «Это **сущность** или **значение**?»

#### **Struct (тип-значение)**
- В **стеке**
- **Копируется** при присваивании
- Без накладных расходов GC
- Для **малых** данных (<16 байт как ориентир)

**Пример**:
```csharp
public struct Point
{
    public int X { get; }
    public int Y { get; }
}

var p1 = new Point(10, 20);
var p2 = p1; // Копия — p2 независима
```

#### **Class (ссылочный тип)**
- В **куче**
- **Ссылка** при присваивании
- Управляется GC
- Сущности с идентичностью

**Правило**: Struct — <16 байт, неизменяемый; Class — во всех остальных случаях.

---

### Record (C# 9+)

Семантика значений для ссылочных типов + иммутабельность.

```csharp
public record UserDto(int Id, string Name, string Email);

var dto1 = new UserDto(1, "John", "john@test.com");
var dto2 = new UserDto(1, "John", "john@test.com");
Console.WriteLine(dto1 == dto2); // TRUE — value equality!

var dto3 = dto1 with { Name = "Jane" }; // Новый record
```

**Идеально для**: DTO, Value Objects, иммутабельные данные.

---

### Readonly vs Const

| | Const | Readonly |
|---|---|---|
| **Когда** | Compile-time | Runtime |
| **Где** | Встраивается в код | Конструктор |
| **Типы** | Примитивы | Любые |
| **Использование** | Константы | DI, конфигурация |

---

### Boxing и Unboxing — скрытая цена

```csharp
int number = 42;           // Value type
object boxed = number;     // Boxing — выделение в куче!
int unboxed = (int)boxed;  // Unboxing
```

**Почему плохо**: лишние аллокации, давление на GC.

**Решение**: использовать Generics (`List<int>` вместо `ArrayList`).

---

## 3. Управление памятью {#memoria}

### Stack vs Heap

**Stack** (LIFO, поток-локальный):
- Локальные переменные, параметры
- Быстрый, предсказуемый
- ~1 МБ на поток

**Heap** (общий, под управлением GC):
- Объекты-ссылки
- Массивы, строки
- Гибкий, но недетерминированное освобождение

### Garbage Collector — поколения

- **Gen 0** — молодые объекты, сборка часто
- **Gen 1** — пережившие Gen 0
- **Gen 2** — долгоживущие, сборка редко
- **LOH** — объекты >85 КБ

**Практика**: избегать «среднеживущих» объектов — они дают нагрузку на GC.

---

### IDisposable — ресурсы вне управления GC

```csharp
public class DatabaseConnection : IDisposable
{
    private bool _disposed = false;
    
    public void Dispose()
    {
        Dispose(true);
        GC.SuppressFinalize(this);
    }
    
    protected virtual void Dispose(bool disposing)
    {
        if (_disposed) return;
        if (disposing) _connection?.Dispose();
        _disposed = true;
    }
}

// Использование
using var connection = new DatabaseConnection(connString);
```

**На собеседовании**:
> «IDisposable — для освобождения неуправляемых ресурсов (файлы, соединения). Использую `using` для гарантированного вызова Dispose. Важно для предотвращения утечек в серверных приложениях».

---

## 4. LINQ {#linq-potere}

### Философия

**Без LINQ** — императивный, многословный код с временными коллекциями.

**С LINQ** — декларативный, компонуемый:

```csharp
var names = users
    .Where(u => u.Age >= 18)
    .Select(u => u.Name)
    .OrderBy(n => n)
    .ToList();
```

### Deferred Execution

Запрос выполняется при **перечислении**, не при объявлении.

```csharp
var query = users.Where(u => u.Age > 18); // Пока ничего не выполняется
foreach (var u in query) { } // Теперь выполняется
```

### IEnumerable vs IQueryable

- **IEnumerable** — в памяти, LINQ to Objects
- **IQueryable** — Expression Trees, выполнение на стороне БД (EF Core, SQL)

**Осторожно**: `.ToList()` или другой материализующий метод — и IQueryable превращается в IEnumerable, запрос уходит в память.

### First vs FirstOrDefault vs Single vs SingleOrDefault

| Метод | Когда нет элементов | Когда больше одного |
|-------|---------------------|----------------------|
| First | Exception | Берёт первый |
| FirstOrDefault | default | Берёт первый |
| Single | Exception | Exception |
| SingleOrDefault | default | Exception |

### Any() vs Count() > 0

`Any()` — останавливается на первом найденном.  
`Count()` — перечисляет всю коллекцию.  
Для проверки наличия — **Any()**.

### Проблема N+1

**Проблема**: Для каждой сущности отдельный запрос к БД.

**Решения**:
1. Eager Loading: `.Include(x => x.Orders)`
2. Projection: `.Select(x => new { x.Id, x.Name, Orders = x.Orders })`

---

# ЧАСТЬ 2: Асинхронность и многопоточность

## 5. Async/Await {#async-rivoluzione}

### Идея

При операции I/O **освобождать поток** вместо блокировки.

**Синхронно**: поток блокируется на 300 мс ожидания БД и API.  
**Асинхронно**: поток занят ~10 мс полезной работой, остальное время обслуживает другие запросы.

**Эффект**: существенный рост пропускной способности при тех же потоках.

### CPU-bound vs I/O-bound

- **I/O-bound** (БД, HTTP, файлы) → `async/await`
- **CPU-bound** (тяжёлые вычисления) → `Task.Run()`

### Deadlock

Частая причина — захват `SynchronizationContext`.  
**Решение**: `ConfigureAwait(false)` в библиотечном коде (не в ASP.NET Core controllers).

### Task.WhenAll / Task.WhenAny

```csharp
var results = await Task.WhenAll(task1, task2, task3);
var first = await Task.WhenAny(task1, task2); // Первый завершённый
```

### CancellationToken

```csharp
public async Task ProcessAsync(CancellationToken ct = default)
{
    await _httpClient.GetAsync(url, ct);
}
```

---

## 6. Многопоточность {#multithreading}

### Async vs Multithreading

- **Async** — один поток, переключение на другие задачи при ожидании I/O
- **Multithreading** — несколько потоков для параллельных вычислений

### lock, Interlocked, Concurrent Collections

- **lock** — критическая секция
- **Interlocked** — атомарные операции
- **ConcurrentDictionary**, **ConcurrentBag** — потокобезопасные коллекции без ручного lock

---

# ЧАСТЬ 3: ASP.NET Core

## 1. Request Pipeline {#aspnet-pipeline}

Каждый HTTP-запрос проходит через **цепочку middleware**.

**Порядок** (важен):
1. Exception handling
2. HTTPS Redirect
3. Static Files
4. Routing
5. Authentication
6. Authorization
7. Endpoint (Controller)

---

## 2. Dependency Injection {#di-filosofia}

### Три времени жизни

| Lifetime | Поведение | Когда использовать |
|----------|-----------|---------------------|
| **Transient** | Новый экземпляр при каждом запросе | Лёгкие stateless сервисы |
| **Scoped** | Один экземпляр на HTTP-запрос | DbContext, Unit of Work |
| **Singleton** | Один на всё приложение | Конфигурация, кеш |

**Важно**: Scoped сервис нельзя инжектить в Singleton.

---

## 3. REST API {#rest-api}

### HTTP-методы

| Метод | Назначение |
|-------|------------|
| GET | Получить |
| POST | Создать |
| PUT | Заменить полностью |
| PATCH | Частичное обновление |
| DELETE | Удалить |

### Коды ответов

- 200 — OK
- 201 — Created
- 204 — No Content
- 400 — Bad Request
- 401 — Unauthorized (кто ты?)
- 403 — Forbidden (доступ запрещён)
- 404 — Not Found
- 500 — Internal Server Error

---

## 4. JWT и авторизация {#autenticazione}

**Структура JWT**: Header.Payload.Signature

**Authentication** — определение личности.  
**Authorization** — проверка прав доступа.

---

# ЧАСТЬ 4: Персистентность данных

## SQL Server {#sql-performance}

### JOIN

- INNER JOIN — только совпадения
- LEFT JOIN — все слева + совпадения справа
- RIGHT JOIN — наоборот
- FULL OUTER — объединение обеих сторон

### Индексы

- Clustered — определяет физический порядок
- Non-clustered — отдельная структура с указателями
- Composite — несколько столбцов

### Уровни изоляции

Read Uncommitted, Read Committed, Repeatable Read, Serializable — компромисс между согласованностью и конкурентностью.

---

## Entity Framework Core {#ef-core}

### DbContext

Центральный объект для работы с БД, кеширования, отслеживания изменений.

### Migrations

```bash
dotnet ef migrations add MigrationName
dotnet ef database update
```

### Include vs AsNoTracking

- `Include` — Eager Loading связанных данных
- `AsNoTracking` — только чтение, без Change Tracker (быстрее)

---

# ЧАСТЬ 5: Архитектура и паттерны

## SOLID {#solid}

1. **S**ingle Responsibility — один класс, одна ответственность
2. **O**pen/Closed — открыт для расширения, закрыт для изменения
3. **L**iskov Substitution — подтипы заменяемы базовым типом
4. **I**nterface Segregation — узкие интерфейсы лучше одного большого
5. **D**ependency Inversion — зависимость от абстракций, не от конкретики

---

## Design Patterns {#patterns}

- **Repository** — абстракция доступа к данным
- **Strategy** — взаимозаменяемые алгоритмы
- **Factory** — создание объектов без знания конкретных классов
- **Observer** — уведомление подписчиков о событиях
- **Decorator** — добавление поведения без изменения класса

---

# ЧАСТЬ 7: Quick Q&A — 70 типичных вопросов {#domande}

> **Совет**: Отвечайте вслух за 30–60 секунд — как на собеседовании.

---

## Фундаменты C# и .NET (Q1–Q20)

### Q1: Четыре принципа ООП?
> **Инкапсуляция** — скрытие реализации. **Наследование** — отношения IS-A. **Полиморфизм** — работа через общий интерфейс. **Абстракция** — моделирование сути, без деталей.

### Q2: Value Type vs Reference Type?
> Value (struct, int) — в стеке, копируется при присваивании. Reference (class) — в куче, передаётся по ссылке. Value не может быть null, Reference — может.

### Q3: Stack vs Heap?
> **Stack** — LIFO, локальные переменные, параметры, ~1 МБ на поток. **Heap** — объекты-ссылки, управляется GC, общий для потоков.

### Q4: Как работает Garbage Collector?
> Поколения (Gen 0/1/2): молодые объекты собираются часто, старые — редко. LOH — объекты >85 КБ. Бóльшая часть объектов умирает в Gen 0.

### Q5: IDisposable и using?
> IDisposable — для освобождения неуправляемых ресурсов (файлы, соединения). `using` гарантирует вызов Dispose даже при исключении. Паттерн: Dispose(), Dispose(bool), при необходимости финализатор.

### Q6: Interface vs Abstract Class?
> **Interface** — контракт, только сигнатуры, множественная реализация. **Abstract Class** — может иметь реализацию и поля, одно наследование. Interface для DI и слабой связанности, Abstract Class для общей логики IS-A.

### Q7: Virtual vs Override vs New?
> **virtual** — метод можно переопределить. **override** — переопределяет базовый. **new** — скрывает базовый (другая сигнатура), не полиморфизм.

### Q8: Sealed class и sealed method?
> **sealed class** — запрещает наследование. **sealed method** — запрещает дальнейший override. Используется для безопасности, производительности, явного замысла.

### Q9: Boxing и Unboxing?
> **Boxing** — неявное преобразование value type в object (аллокация в куче). **Unboxing** — обратное извлечение. Избегать: использовать Generics вместо non-generic коллекций.

### Q10: Nullable value types?
> `int?` = `Nullable<int>`. Позволяет value type быть null. `.HasValue`, `.Value`, оператор `??` для значений по умолчанию.

### Q11: IEnumerable vs IQueryable?
> **IEnumerable** — в памяти (LINQ to Objects). **IQueryable** — Expression Trees, выполнение на стороне БД (EF Core). `.ToList()` материализует IQueryable и переводит в память.

### Q12: Deferred execution в LINQ?
> Запрос выполняется при перечислении, не при объявлении. Повторное перечисление = повторное выполнение. `.ToList()`, `.Count()` и т.п. — немедленное выполнение.

### Q13: Select vs SelectMany?
> **Select** — проекция 1:1. **SelectMany** — «разворачивает» вложенные коллекции в плоский результат (flatten).

### Q14: Where + ToList vs ToList + Where?
> `Where().ToList()` — фильтр в БД (IQueryable). `ToList().Where()` — загрузка всего в память, фильтр в C#. Первый вариант обычно предпочтительнее.

### Q15: Проблема N+1 в LINQ?
> Загрузка N сущностей и отдельный запрос для каждой связанной — 1+N запросов. Решение: Eager Loading (`.Include()`) или проекция (`.Select()`).

### Q16: Как работает async/await?
> Компилятор создаёт state machine. При `await` поток освобождается, при завершении операции продолжает выполнение (возможно на другом потоке). Не блокирует поток во время ожидания I/O.

### Q17: Task vs Thread?
> **Thread** — низкоуровневый поток ОС. **Task** — единица работы, планируется ThreadPool. Task — рекомендуемая абстракция; async/await работает с Task.

### Q18: ConfigureAwait(false)?
> Отключает захват SynchronizationContext при продолжении. В библиотечном коде — меньше риска deadlock. В ASP.NET Core контроллерах обычно не нужен (нет context).

### Q19: Deadlock и как избежать?
> Взаимная блокировка потоков. Часто из‑за `SyncContext` в UI/WinForms. Решение: `ConfigureAwait(false)` в библиотеках, избегать `.Result` и `.Wait()`.

### Q20: CancellationToken?
> Позволяет отменить асинхронную операцию. Передаётся в async-методы. `ct.ThrowIfCancellationRequested()`. Используется для timeout, отмены из UI.

---

## ASP.NET Core и архитектура (Q21–Q35)

### Q21: Request Pipeline в ASP.NET Core?
> Цепочка middleware для каждого HTTP-запроса. Каждый обрабатывает request/response и решает передать дальше или прервать. Важен порядок: Exception → HTTPS → Static → Routing → Auth → Endpoints.

### Q22: Что такое Middleware?
> Компонент с `RequestDelegate _next`. Выполняет логику до и после `await _next(context)`. Может прервать pipeline, не вызывая _next. Примеры: логирование, обработка ошибок, CORS.

### Q23: DI: Scoped vs Singleton vs Transient?
> **Transient** — новый экземпляр при каждом запросе. **Scoped** — один на HTTP-запрос (DbContext). **Singleton** — один на приложение. DbContext — всегда Scoped.

### Q24: Принципы REST?
> Ресурсы как URI, HTTP-глаголы (GET/POST/PUT/PATCH/DELETE), Stateless, представления (JSON/XML). Коды ответов по семантике.

### Q25: HTTP Status Codes?
> 200 OK, 201 Created, 204 No Content, 400 Bad Request, 401 Unauthorized (кто ты?), 403 Forbidden (нет прав), 404 Not Found, 500 Internal Server Error.

### Q26: JWT Authentication?
> Токен из 3 частей: Header.Payload.Signature. Payload — claims (user id, roles). Подпись секретом. Stateless — сервер не хранит сессию. Client: `Authorization: Bearer <token>`.

### Q27: Claims-based Authorization?
> Авторизация по claims в токене (роли, права). Политики: `[Authorize(Policy = "Admin")]`. Можно проверять несколько claims.

### Q28: CORS?
> Cross-Origin Resource Sharing. Разрешает вызовы API с другого домена. Настраивается в `AddCors()`. В проде указывать конкретные Origins, не `AllowAnyOrigin()`.

### Q29: Model Binding?
> Автоматическое сопоставление данных запроса (query, body, route) с параметрами action. `[FromBody]`, `[FromQuery]`, `[FromRoute]`.

### Q30: Action Filters?
> Логика до/после выполнения action. `IActionFilter` — `OnActionExecuting`, `OnActionExecuted`. Для логирования, валидации, кеширования.

### Q31: Принципы SOLID?
> **S** — одна ответственность. **O** — открыт для расширения, закрыт для изменения. **L** — подтипы заменяемы базовым. **I** — узкие интерфейсы. **D** — зависимость от абстракций.

### Q32: Dependency Inversion Principle?
> Зависимость от абстракций (интерфейсов), а не от конкретных классов. Реализуется через DI. Модули верхнего уровня не зависят от нижних.

### Q33: Repository Pattern?
> Абстракция доступа к данным. Интерфейс в domain/application, реализация в infrastructure. Тестируемость, замена ORM, единая точка доступа.

### Q34: Strategy Pattern?
> Семейство взаимозаменяемых алгоритмов. Один интерфейс, разные реализации. Меняется поведение в runtime без изменения вызывающего кода.

### Q35: Factory Pattern?
> Создание объектов без знания конкретного класса. Client получает объект через фабрику. Скрывает логику создания.

---

## База данных и производительность (Q36–Q44)

### Q36: EF Core Migrations?
> Версионирование схемы БД. `dotnet ef migrations add Name` — Up/Down. `dotnet ef database update` — применение. В проде часто используют SQL-скрипты.

### Q37: Lazy vs Eager Loading?
> **Eager** (`.Include()`) — загрузка связей в одной query. **Lazy** — загрузка при обращении к navigation property (отдельные запросы). Eager обычно предпочтительнее (N+1).

### Q38: Проблема N+1 в EF Core?
> Загрузка N сущностей + отдельный запрос для каждой связи. Решение: `.Include()`, проекция `.Select()`, иногда explicit loading.

### Q39: Когда использовать AsNoTracking?
> Для read-only запросов (API GET, отчёты). Отключает Change Tracker — быстрее и меньше памяти. Не использовать, если потом нужно `SaveChanges()`.

### Q40: Как работают индексы в SQL?
> B-Tree ускоряет поиск (O(log n) вместо O(n)). Создавать на колонках в WHERE, JOIN, ORDER BY. Clustered — один на таблицу (физический порядок), Non-clustered — несколько.

### Q41: Почему string неизменяем?
> Thread-safety, стабильность hash code (Dictionary/HashSet). Возможность string interning. Изменения создают новый объект — для конкатенаций в цикле лучше StringBuilder.

### Q42: List vs Array?
> **Array** — фиксированный размер. **List** — динамический рост. Array быстрее, List гибче с методами Add, Remove, Sort.

### Q43: Dictionary vs List для поиска?
> **Dictionary** — O(1) lookup по ключу. **List** — O(n) линейный поиск. Dictionary для cache, mapping; List для упорядоченных данных без ключа.

### Q44: Когда использовать HashSet?
> Уникальные значения, O(1) lookup. Автоматически убирает дубликаты. Удаление дублей, проверка существования, операции множеств (Union, Intersect).

---

## Дополнительные Q&A (Q45–Q55)

### Q45: Include vs Explicit Loading?
> **Include** — Eager Loading, одна query с JOIN. **Explicit** — отдельные запросы по требованию (`Load()`). Include, когда данные почти всегда нужны; Explicit — когда редко.

### Q46: PUT vs PATCH?
> **PUT** — полная замена ресурса, идемпотентный. **PATCH** — частичное обновление, только указанные поля. PUT — весь body; PATCH — `{"email": "new@test.com"}`.

### Q47: 401 vs 403?
> **401** — не аутентифицирован (нет/неверный токен). **403** — аутентифицирован, но прав на действие нет. Без токена → 401; с токеном User к Admin-endpoint → 403.

### Q48: FirstOrDefault vs SingleOrDefault?
> **FirstOrDefault** — первый или null, допустимо несколько. **SingleOrDefault** — ровно один или null, иначе exception. First для упорядоченных списков, Single для уникальных ключей.

### Q49: Cache-Aside (Lazy Loading)?
> Проверка кеша → при miss загрузка из БД → запись в кеш с TTL → возврат. Кеш заполняется по запросу. При обновлении БД — инвалидация кеша.

### Q50: RabbitMQ vs Kafka?
> **RabbitMQ** — message broker, сообщения удаляются после потребления, маршрутизация. **Kafka** — event streaming, хранение, replay. RabbitMQ — workflow; Kafka — высокий throughput, audit, replay.

### Q51: Микросервисы vs Монолит?
> Микросервисы — при большой команде, независимом масштабировании, сложном домене. Минусы — сетевая латентность, распределённая согласованность. Начинать с хорошо структурированного монолита.

### Q52: Isolation Level в SQL?
> Контролирует видимость данных между транзакциями. Read Committed — по умолчанию. Repeatable Read — повторяемые чтения. Serializable — максимальная изоляция. Snapshot (MVCC) — без блокировок при чтении.

### Q53: Strategy vs Factory?
> **Strategy** — взаимозаменяемые алгоритмы (одинаковое действие, разная реализация). **Factory** — создание объектов (клиент не знает конкретный класс). Strategy — поведение, Factory — создание.

### Q54: Unit vs Integration Tests?
> **Unit** — изолированно, с mock’ами, быстро. **Integration** — с реальными зависимостями (БД, API), медленнее. Unit для бизнес-логики, Integration для сквозных сценариев.

### Q55: Decorator vs Inheritance?
> **Decorator** — добавление поведения через композицию, без изменения класса. **Inheritance** — жёсткая иерархия. Decorator гибче, лучше «composition over inheritance».

### Q56: Circuit Breaker?
> «Предохранитель» для внешних вызовов. После N неудач — открыт, вызовы сразу падают. Потом half-open — пробный вызов. Закрывается при успехе. Защита от каскадных сбоев.

### Q57: Window Functions vs GROUP BY?
> **GROUP BY** — агрегация, одна строка на группу. **Window Functions** — агрегация с сохранением всех строк. GROUP BY «сворачивает»; Window — детали + агрегат в одной строке.

### Q58: EF Core vs Dapper?
> **EF Core** — ORM, change tracking, migrations, LINQ. **Dapper** — micro-ORM, raw SQL → объекты. Dapper быстрее; EF удобнее для CRUD. Dapper — сложные запросы, отчёты, bulk.

### Q59: Options Pattern?
> Конфигурация в строго типизированные классы. `Configure<MySettings>()`, инъекция `IOptions<MySettings>`. IOptionsSnapshot — per-request refresh; IOptionsMonitor — hot reload.

### Q60: Background Service / IHostedService?
> Запускается со стартом приложения. `BackgroundService` — базовая реализация с `ExecuteAsync`. Для планировщиков, обработки очередей. Важно обрабатывать исключения.

### Q61: Unit of Work?
> Группировка нескольких операций в одну транзакцию. В EF Core DbContext уже Unit of Work: `SaveChangesAsync()` сохраняет всё атомарно.

### Q62: Soft Delete?
> Поле `IsDeleted`, `DeletedAt` вместо физического удаления. Global Query Filter в OnModelCreating исключает удалённые. `.IgnoreQueryFilters()` для доступа к ним.

### Q63: Merge vs Rebase?
> **Merge** — merge commit, полная история. **Rebase** — линейная история, переписывание. Merge для feature→develop; Rebase для обновления своей ветки. Не rebase публичных веток.

### Q64: Daily Standup?
> 15 минут, три вопроса: что сделал вчера, что сделаю сегодня, есть ли блокеры. Не отчёт менеджеру, а синхронизация команды.

### Q65: User Story?
> Формат: «Как [роль] я хочу [функцию], чтобы [выгода]». Acceptance Criteria определяют «готово». Оценка — Story Points (сложность, не часы).

### Q66: Git Flow?
> main (production) и develop. feature/ от develop, hotfix/ от main. feature → PR → develop → release → main. Naming: feature/TICKET-123-description.

### Q67: Merge conflict?
> Открыть файлы с маркерами <<<<<<<, =======, >>>>>>>. Выбрать нужный вариант или объединить. Удалить маркеры, протестировать, git add, commit, push.

### Q68: Pull Request?
> Запрос на слияние ветки (feature → develop). Code review перед merge. Push ветки, создание PR, ревью, правки, approval, merge.

### Q69: Анализ функциональных требований?
> Уточнение требований, определение акторов, потоков, правил. Выход: документ с preconditions, основным и альтернативными потоками, валидациями, схемой данных, API.

### Q70: Какая документация нужна?
> README (setup), Swagger (API), комментарии для сложной бизнес-логики, схема БД. Принцип: код — главная документация, комментарии — про «почему», а не «что».

---

# ЧАСТЬ 8: Kirey — подготовка к собеседованию Digital Insurance {#kirey}

> **Контекст**: Kirey — европейская IT-консалтинговая компания (~1500 сотрудников). Роль: .NET Developer в **Digital Insurance** (Милан, hybrid/remote).

---

## Требования вакансии — приоритеты

| Приоритет | Технология | Уровень |
|-----------|------------|---------|
| **Обязательно** | .NET, C#, SQL Server | Хорошее знание |
| **Обязательно** | Quartz.NET (scheduling) | Базовое |
| **Обязательно** | WCF | Базовое знакомство |
| **Обязательно** | Teamwork, Problem Solving | Важно |
| **Плюс** | Insurance domain | Знание отрасли |
| **Плюс** | GIT | Владение |

**Обязанности**: функциональный анализ, технический дизайн, разработка, установка и тестирование, обновление документации.

---

## Quartz.NET — подробно {#quartz-scheduling}

### Концепции

```
Trigger (КОГДА) → Job (ЧТО) → Scheduler (оркестратор)
```

### Job — что выполнять

```csharp
public class CalculateMonthlyPremiumsJob : IJob
{
    private readonly IPolicyService _policyService;

    public async Task Execute(IJobExecutionContext context)
    {
        var activePolicies = await _policyService.GetActivePoliciesAsync();
        foreach (var policy in activePolicies)
        {
            await _policyService.CalculatePremiumAsync(policy.Id);
        }
    }
}
```

### Trigger — когда выполнять

**Simple Trigger** — фиксированные интервалы:
```csharp
.WithSimpleSchedule(x => x.WithIntervalInMinutes(30).RepeatForever())
```

**Cron Trigger** — гибкое расписание:
```csharp
.WithCronSchedule("0 0 3 * * ?")  // Каждый день в 3:00
.WithCronSchedule("0 0 9 ? * MON") // Каждый понедельник в 9:00
```

### Cron-выражения (формат Quartz)

`секунды минуты часы день_месяца месяц день_недели`

| Выражение | Значение |
|-----------|----------|
| `0 0 3 * * ?` | Каждый день в 3:00 |
| `0 0 9 ? * MON` | Каждый понедельник в 9:00 |
| `0 */15 * * * ?` | Каждые 15 минут |
| `0 0 0 1 * ?` | 1-е число месяца в полночь |
| `0 0 8 ? * MON-FRI` | Будни в 8:00 |

### Регистрация в ASP.NET Core

```csharp
builder.Services.AddQuartz(q =>
{
    q.UseMicrosoftDependencyInjectionJobFactory();
    var jobKey = new JobKey("MonthlyPremiums");
    q.AddJob<CalculateMonthlyPremiumsJob>(opts => opts.WithIdentity(jobKey));
    q.AddTrigger(opts => opts.ForJob(jobKey)
        .WithCronSchedule("0 0 3 1 * ?")); // 1-е число, 3:00
});
builder.Services.AddQuartzHostedService(q => q.WaitForJobsToComplete = true);
```

**Типичные сценарии в Insurance**: ночные отчёты, расчёт премий, очистка данных, batch-уведомления.

---

## WCF — подробно {#wcf-basics}

### Что это

**WCF** (Windows Communication Foundation) — legacy-фреймворк Microsoft для SOAP-сервисов на XML. Часто встречается в страховых и банковских системах.

### ABC WCF

- **A**ddress — адрес сервиса (`http://server:8080/PolicyService`)
- **B**inding — транспорт (HTTP, TCP, Named Pipes)
- **C**ontract — контракт (интерфейс с операциями)

```csharp
[ServiceContract]
public interface IPolicyService
{
    [OperationContract]
    Policy GetPolicy(string policyNumber);
}

[DataContract]
public class Policy
{
    [DataMember] public string Number { get; set; }
    [DataMember] public decimal Premium { get; set; }
}
```

### Почему устарел

| WCF (SOAP) | REST API |
|------------|----------|
| XML (многословно) | JSON (компактно) |
| Сложная настройка | Простая |
| Ниже производительность | Выше |

**Современная замена**: ASP.NET Core Web API (REST) или gRPC.

**Миграция**: WCF + REST dual hosting, затем постепенный переход клиентов на REST.

---

## Insurance Domain — подробно {#insurance-basics}

### Термины (IT↔EN)

| EN | RU | Описание |
|----|----|----------|
| Policy | Полис | Договор страхования |
| Premium | Премия | Регулярный платёж |
| Claim | Заявление о выплате / Синистр | Запрос на возмещение |
| Underwriting | Андеррайтинг | Оценка риска |
| Deductible | Франшиза | Часть ущерба за счёт клиента |
| Coverage | Покрытие | Что застраховано |
| Policyholder | Страхователь | Кто платит |
| Payout | Выплата | Компенсация от страховой |

### Policy (полис)

- PolicyNumber, Policyholder, Coverage
- Premium (размер платежа)
- Deductible (франшиза)
- StartDate, ExpirationDate, Status (Active/Expired/Cancelled)

### Premium (премия)

- Платёж по расписанию (ежемесячно, ежегодно)
- Зависит от риска: возраст, история, тип покрытия
- В коде: базовый тариф × коэффициенты риска

### Claim (заявление о выплате)

1. Происходит событие (авария, кража и т.п.)
2. Клиент подаёт claim
3. Прикладывает документы
4. Андеррайтинг проверяет
5. Утверждение/отклонение → выплата (минус франшиза)

### Упрощённый расчёт премии

```csharp
// Упрощённо: basePremium * riskMultiplier
// Факторы: возраст водителя, история аварий, тип полиса, франшиза
```

---

## Функциональный анализ (обязанность)

Ожидается: разбирать требования, проектировать, оценивать сроки.

**Шаги**:
1. Сбор требований — уточняющие вопросы Product Owner
2. Анализ — акторы, предусловия, основной поток, альтернативы
3. Правила — валидации, ограничения
4. Модель данных — сущности, связи
5. API — endpoints, DTO
6. Оценка — task breakdown, story points

**Пример для Insurance**: «Открытие claim онлайн» — какие типы полисов, лимиты по времени, нужные документы, кто утверждает.

---

## soft skills — что ценит Kirey

- **Teamwork** — работа в команде, code review, pair programming
- **Problem Solving** — примеры: разбор сложного бага, оптимизация
- **Коммуникация** — понятные объяснения, диалог с бизнесом
- **Гибкость** — готовность к hybrid/remote, изменению задач
- **Ориентация на результат** — завершённые задачи, качество

**Полезно подготовить**: 2–3 коротких примера из опыта (командная работа, сложная задача, компромисс с командой).

---

## Quick Q&A для Kirey

### Q-K1: Quartz.NET — Job vs Trigger?
> **Job** — что выполнять (класс с `Execute`). **Trigger** — когда (расписание). Scheduler объединяет Job и Trigger.

### Q-K2: Cron для «каждый день в 3:00»?
> `0 0 3 * * ?` — секунды=0, минуты=0, часы=3, каждый день, любой месяц, любой день недели.

### Q-K3: WCF — зачем в 2025?
> Для интеграции со старыми страховыми/банковскими системами. Новый код пишут на REST/gRPC.

### Q-K4: Policy vs Claim?
> **Policy** — договор (что застраховано, премия, срок). **Claim** — заявление клиента на выплату по этому договору.

### Q-K5: Deductible (франшиза)?
> Сумма, которую клиент платит сам до начала выплат страховой. ApprovedAmount = ClaimedAmount - Deductible.

### Q-K6: Подход к функциональному анализу?
> Уточняю требования, выделяю акторов, потоки, правила и валидации, проектирую модель и API, оцениваю трудозатраты.

---

## Чеклист перед собеседованием Kirey

**За неделю:**
- [ ] Quartz.NET: Job, Trigger, Scheduler, базовые Cron
- [ ] WCF: ABC, SOAP, причины устаревания
- [ ] Insurance: Policy, Premium, Claim, Underwriting, Deductible

**За 3 дня:**
- [ ] GIT: branch, merge, PR, разрешение конфликтов
- [ ] Agile: Sprint, Daily Standup, User Stories
- [ ] Функциональный анализ: пример по требованию

**За 1 день:**
- [ ] Повторить все 70 Quick Q&A
- [ ] Подготовить 2–3 примера teamwork / problem solving
- [ ] Подготовить вопросы к интервьюеру

---

## Вопросы интервьюеру (Kirey)

**Технологии:**
- Какой стек используется в Digital Insurance? EF Core, Quartz?
- Quartz.NET — для каких задач (batch, отчёты, расчёт премий)?
- Есть ли поддержка legacy WCF или переход на REST?

**Процесс:**
- Как организована команда? Сколько разработчиков?
- Используете Scrum? Длина спринта?
- Как проходит code review?

**Проект:**
- Над какими insurance-проектами работаю? Products, Claims, Underwriting?
- Есть ли legacy или в основном новый код?
- Насколько сложный domain?

**Рост:**
- Какие курсы и сертификации доступны?
- Какие планы развития у Digital Insurance?

---

## Важные акценты

1. **Teamwork** — показать опыт совместной работы, code review, помощи коллегам
2. **Практика** — конкретные примеры (решенные баги, оптимизация, рефакторинг)
3. **Insurance** — интерес к домену: «Изучал, как работает расчёт премий и андеррайтинг»
4. **Адаптивность** — готовность к hybrid, Agile, разным задачам
5. **Документация** — понимание важности README, Swagger, обновления документации

---

# ЧАСТЬ 9: Дополнительные технологии

## gRPC vs REST

| | REST | gRPC |
|---|------|------|
| Формат | JSON/XML | Protobuf |
| Производительность | Ниже | Выше |
| Streaming | Сложнее | Есть |
| Использование | Публичные API, браузер | Внутренние сервисы |

---

## 📋 Quick Q&A — сводка по темам

| Темы | Вопросы |
|------|---------|
| C# и .NET | Q1–Q20 |
| ASP.NET Core | Q21–Q35 |
| База данных | Q36–Q44 |
| Паттерны, кеш, тесты | Q45–Q57 |
| EF, DI, Git, Agile | Q58–Q70 |

---

## 📋 Общий чеклист перед собеседованием

- [ ] Повторить OOP, типы, async/await, LINQ
- [ ] ASP.NET Core: Pipeline, DI, REST, JWT
- [ ] Entity Framework Core, SQL
- [ ] SOLID, Design Patterns
- [ ] Пройти все **70 Quick Q&A** вслух (30–60 сек на ответ)
- [ ] **Для Kirey**: Quartz.NET, WCF, Insurance domain, функциональный анализ — см. [ЧАСТЬ 8](#kirey)

---

**Удачи на собеседовании!** 🚀
