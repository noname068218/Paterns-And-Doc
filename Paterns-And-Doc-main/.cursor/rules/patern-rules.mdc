---
alwaysApply: true
---
# 🎯 Project Purpose

Questo progetto è dedicato all'apprendimento pratico e teorico di C#, .NET, ASP.NET Core, React (TypeScript/JavaScript), algoritmi e architettura software.  
Serve come sandbox personale per migliorare le mie competenze di programmazione, sperimentare con vari pattern, testare nuove librerie e applicare concetti teorici nella pratica.

# 🧠 Project Context (контекст проекта)

- Это **учебный проект**, созданный для тренировки навыков C# / .NET / ASP.NET / React (TypeScript, JavaScript) / алгоритмов.
- Основная цель — **понять, как всё работает в деталях**, а не просто написать код.
- В проекте я пробую различные подходы, паттерны, архитектурные решения и практики.
- Все примеры, комментарии и документация в коде — **на английском языке** (согласно user_rules).
- Все объяснения и текстовые ответы в чате — **на русском языке**.
- Код и структура должны быть **профессиональными и чистыми**, даже если проект не продакшен.

# 🧩 Technical Guidelines (технические указания)

## Backend (C# / .NET)

- Используй **C# 12 / .NET 8**.
- Придерживайся принципов **SOLID, DRY, Clean Code, KISS, YAGNI**.
- Если уместно, применяй **Clean Architecture, CQRS, DDD-подход, Mediator Pattern**.
- В ASP.NET Core разделяй слои: `Domain`, `Application`, `Infrastructure`, `WebAPI`.
- Используй **async/await** везде, где это возможно (I/O операции, вызовы API, работа с БД).
- Применяй **Dependency Injection** для всех зависимостей.
- Всегда добавляй **валидацию входных данных** (FluentValidation, Data Annotations).
- Используй **Entity Framework Core** для работы с БД, предпочитай Code First подход.
- Применяй **Repository Pattern** или **Unit of Work** для абстракции доступа к данным.
- Обрабатывай ошибки централизованно через **Exception Middleware** и логируй их.
- Используй **AutoMapper** или **Mapster** для маппинга объектов.

## Frontend (React / TypeScript)

- Используй **React 18+** с **TypeScript 5+**.
- Предпочитай **Functional Components** с **Hooks** (useState, useEffect, useCallback, useMemo, useContext, useReducer).
- Применяй **React Query (TanStack Query)** для управления состоянием сервера и кэширования.
- Используй **Zustand** или **Context API** для глобального состояния при необходимости.
- Следуй принципам **Component Composition** и **Separation of Concerns**.
- Всегда типизируй пропсы, события и состояния через **TypeScript interfaces/types**.
- Используй **React.memo**, **useMemo**, **useCallback** для оптимизации производительности где уместно.
- Применяй **Custom Hooks** для переиспользуемой логики.
- Используй **React Router** для навигации.
- Формы обрабатывай через **React Hook Form** с **Yup** или **Zod** валидацией.
- Используй **Axios** или **Fetch API** для HTTP запросов к backend.
- Структурируй компоненты по принципу: `components/`, `hooks/`, `services/`, `types/`, `utils/`.
- Применяй **CSS Modules**, **Styled Components** или **Tailwind CSS** для стилизации.

## Алгоритмы и структуры данных

- Для примеров алгоритмов — пиши **пошаговые комментарии на английском**, объясняя логику.
- Для каждой демонстрации добавляй короткое резюме на английском: what it does, why it's useful, time/space complexity (Big-O notation).
- Показывай различные подходы: наивный, оптимизированный, альтернативные решения.
- Объясняй выбор структуры данных (List vs Array, Dictionary vs HashSet, Stack vs Queue и т.д.).
- Демонстрируй работу с алгоритмами сортировки, поиска, обхода графов и деревьев.

# 💬 Language Rules (правила языка)

- **Все комментарии в коде, документация и XML-docs** — на **английском** языке.
- **Все объяснения и текстовые ответы в чате** — на **русском** языке.
- Все названия переменных, классов, методов, функций, компонентов — на **английском** языке (PascalCase / camelCase / kebab-case для CSS).
- Имена переменных должны быть **описательными и понятными** (избегай сокращений кроме общепринятых).
- Используй английский для всех **Console.WriteLine**, **Debug.Log**, **throw new Exception** сообщений.

# 🧰 Coding Style

## C# Coding Style

- Используй `async/await` там, где это возможно (I/O операции).
- Обязательно добавляй **XML-документацию (`///`)** к каждому публичному методу, классу, свойству, enum.
- Следи за структурой: **один класс — одна ответственность** (Single Responsibility Principle).
- Используй **expression-bodied members** где это улучшает читаемость.
- Предпочитай **readonly** поля где возможно.
- Используй **var** когда тип очевиден из правой части, иначе явно указывай тип.
- Применяй **null-conditional** и **null-coalescing** операторы (`?.`, `??`, `??=`).
- Используй **pattern matching** (C# 8+) для более читаемого кода.
- Для тестов используй **xUnit**, **FluentAssertions**, **Moq/NSubstitute**.
- Если уместно, показывай **варианты оптимизации** и объясняй их на русском.

## TypeScript/React Coding Style

- Используй **strict mode** TypeScript.
- Всегда указывай типы для параметров функций и возвращаемых значений (избегай `any`).
- Используй **const assertions** где возможно.
- Применяй **destructuring** для пропсов и объектов.
- Используй **arrow functions** для компонентов где это уместно.
- Всегда обрабатывай **error states** и **loading states** в асинхронных операциях.
- Используй **ESLint** и **Prettier** для форматирования кода.
- Следуй **Airbnb** или **Standard** style guide для React/TypeScript.

# 🧪 Learning Focus

## Backend Topics

- **Основы C# и ООП**: наследование, полиморфизм, инкапсуляция, абстракция
- **LINQ**: запросы, методы расширения, deferred execution
- **Entity Framework Core**: миграции, отношения, eager/lazy loading, change tracking
- **ASP.NET Core Web API**: контроллеры, действия, маршрутизация, модель привязки
- **Middleware**: pipeline, создание кастомных middleware, порядок выполнения
- **Dependency Injection**: регистрация сервисов, scope lifetime, сервис локатор (антипаттерн)
- **Configuration**: appsettings.json, environment variables, options pattern
- **Unit Testing**: xUnit, Moq, тестирование контроллеров, сервисов, repositories
- **Design Patterns**: Singleton, Factory, Strategy, Observer, Repository, Unit of Work, Mediator, CQRS
- **Алгоритмы и структуры данных**: сортировки, поиск, деревья, графы, хэш-таблицы, Big-O notation
- **Асинхронность**: async/await, Task, Task<T>, Parallel LINQ, lock, SemaphoreSlim, Concurrent collections
- **Обработка ошибок**: try-catch, Exception filters, Global exception handler, логирование (Serilog, NLog)
- **Безопасность**: JWT authentication, authorization policies, HTTPS, CORS, SQL injection prevention
- **Производительность**: кэширование (MemoryCache, Redis), профилирование, оптимизация запросов

## Frontend Topics

- **React Basics**: компоненты, пропсы, состояние, жизненный цикл, события
- **React Hooks**: useState, useEffect, useContext, useReducer, useMemo, useCallback, custom hooks
- **TypeScript**: типы, интерфейсы, generics, utility types, type guards, discriminated unions
- **State Management**: локальное состояние, Context API, Zustand, Redux Toolkit (если нужно)
- **Data Fetching**: Fetch API, Axios, React Query (TanStack Query), обработка ошибок
- **Routing**: React Router, защищённые маршруты, параметры маршрута, навигация программно
- **Forms**: управляемые/неуправляемые компоненты, React Hook Form, валидация (Yup, Zod)
- **Styling**: CSS Modules, Styled Components, Tailwind CSS, responsive design
- **Performance**: React.memo, useMemo, useCallback, code splitting, lazy loading
- **Testing**: Jest, React Testing Library, тестирование компонентов и хуков
- **Architecture**: структура папок, переиспользуемые компоненты, separation of concerns

# 🤖 AI Assistant Requirements (требования к AI-ассистенту)

## Общие требования

- **Всегда добавляй подробные комментарии на английском** в коде, объясняя каждый метод, класс и шаг алгоритма построчно.
- **Всегда объясняй логику структурировано** на русском в чате: сначала общая идея, затем построчное или блочное объяснение.
- **Считай пользователя начинающим разработчиком** с опытом ~2 года, стремящимся стать Middle по C#/.NET, ASP.NET Web API, React (TypeScript, JavaScript).
- **Оптимизируй код** для читаемости, сопровождаемости и масштабируемости.
- **В чате всегда отвечай на русском** и разъясняй все шаги на русском (даже если код и комментарии в коде — на английском).
- **Показывай варианты оптимизации** и объясняй их, когда уместно.
- **Всегда объясняй "почему"** выбран тот или иной подход, паттерн, структура данных.
- **Предлагай альтернативные решения** и объясняй их преимущества и недостатки.

## При написании кода

- **Пиши чистый, самодокументируемый код** с понятными именами переменных и методов.
- **Добавляй XML-документацию** ко всем публичным API (/// comments).
- **Включай inline комментарии** для сложной логики, алгоритмов, неочевидных решений.
- **Добавляй примеры использования** в XML-документации где уместно.
- **Показывай обработку ошибок** и edge cases.
- **Добавляй валидацию** входных параметров и данных.
- **Используй современные возможности C# 12 / .NET 8** и TypeScript 5+ / React 18+.

## При объяснении в чате

- **Объясняй концепции простым языком**, избегая излишнего жаргона без объяснений.
- **Давай контекст**: что это, зачем нужно, когда использовать, когда не использовать.
- **Показывай примеры** практического применения.
- **Объясняй trade-offs**: плюсы и минусы выбранного подхода.
- **Ссылайся на best practices** и принципы (SOLID, DRY, KISS и т.д.).
- **Предупреждай об антипаттернах** и частых ошибках.
- **Структурируй ответы**: используй списки, заголовки, примеры кода с объяснениями.

## При работе с алгоритмами

- **Всегда указывай временную и пространственную сложность** (Big-O notation).
- **Сравнивай различные подходы** к решению одной задачи.
- **Показывай визуализацию** алгоритма в виде комментариев или псевдокода.
- **Объясняй выбор структуры данных** для конкретной задачи.
- **Демонстрируй оптимизации** и объясняй, как они улучшают производительность.

## При работе с React/TypeScript

- **Объясняй lifecycle компонентов** и когда какие хуки использовать.
- **Показывай типизацию** TypeScript и объясняй, почему выбран тот или иной тип.
- **Объясняй паттерны React**: composition, render props, higher-order components.
- **Показывай обработку асинхронных операций** и ошибок в React.
- **Объясняй оптимизацию производительности**: memoization, code splitting, lazy loading.

# ✅ Example Behavior (пример ожидаемого поведения)

> When writing code, always add detailed comments in English explaining what each method, class, and algorithm step does line by line.  
> When responding in chat, explain in Russian what this code does, why it's needed, and how it works.  
> Structure your explanations: first the general idea, then line-by-line or block-by-block explanation.  
> Always explain the "why" behind your choices: why this pattern, why this data structure, why this approach.
